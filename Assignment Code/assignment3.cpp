//Marc Hayek
//200902319
//#include<iostream>
//using namespace std;
//#include<cmath>
//
//void main()
//{
	//Excersise 1
	    /*int counter;
		double number;
		double large;
		
		counter=0;

		cout<<"Enter 10 numbers"<<endl;
		cin>>number;
		counter++;
		large=number;

		while(counter<10)
		{
			cin>>number;
			counter++;
			if(number>large)
				large=number;
		}
		
		cout<<"The largest number you entered is:"<<large<<endl;*/
	

	//Excersise 2
		/*int side1;
		int side2;
		int hyp;
		int results=0;
		

		cout<<"Side 1\tSide 2\tHypotenuse"<<endl<<endl;
		
		for(side1=1;side1<=500;side1++)
		{ 
			for(side2=1;side2<=500;side2++)
			{
				for(hyp=1; hyp<=500;hyp++)
				{

					if((side1*side1)+(side2*side2)==(hyp*hyp))

					{cout<<side1<<"\t"<<side2<<"\t"<<hyp<<endl<<endl;
					results++;}
				}
			}
		}
		cout<<"The number of results is:"<<results<<endl<<endl;				*/



	//Excersise 3
		/*double sum;
		double add;
		add=3;
		for(sum=1;add<=(151);(add=add+2))
			sum=sum+(1/add);
		cout<<"The sum is: "<<sum<<endl;*/



	//Excersise 4
	/*
		int m = 12344, n = 16624, counter = 0;		
		int divisor;

		cout<<"The divisors are:\n";
		
		
		for(divisor=1; divisor<=m; divisor++)
		{
			if (  ( (m%divisor) == 0 ) && ( (n%divisor) == 0 )  )
			{cout<<divisor<<",";
			counter++;}
			
		}
		
		cout<<"\nTherefore, the number of common divisors between "<<m<<" and "<<n<<" is: "<<counter<<endl;*/

	//Excersise 5

	//double x;
	//double n;
	//double sum=1;
	//double m=1;
	////double f=1;

	//
	//cout<<"Enter a value for x to calculate exp(-x): ";
	//cin>>x;
	//n=1;

	//for(sum=1;n<50;n++)
	//{m=m*(-x/n);
	//sum=sum+m;}
	/*{
		while(m!=n)
		{m++;
		f=(f*m);
				}*/

	/*sum=sum+((pow(-1,n))*((pow(x,n))/(f)));*/
	
	/*}*/
//
//	cout<<"The sum is: "<<sum<<endl;
//	cout<<"Check with actual value:"<<exp(-x)<<endl;
//
//
//}